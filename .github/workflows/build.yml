name: Builds
on:
  push:
    branches:
      - '**'

jobs:
  inspect:
    runs-on: windows-11-arm
    steps:
      - name: Run testcase
        run: |
          curl -LO https://martin.st/temp/unwind-packed-bugs.exe
          ./unwind-packed-bugs.exe
      - name: Check version
        shell: cmd
        run: |
          ver
