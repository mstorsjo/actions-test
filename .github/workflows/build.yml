name: Builds
on:
  push:
    branches:
      - '**'

jobs:
  inspect:
    strategy:
      fail-fast: false
      matrix:
        runner:
          - windows-latest
          - windows-11-arm
    runs-on: ${{matrix.runner}}
    steps:
      - name: Inspect 7zip
        shell: cmd
        run: |
          where 7z
          7z i
