name: Builds
on:
  push:
  pull_request:

jobs:
  test-msvc-wine-macos:
    runs-on: macos-latest
    steps:
      - name: Install prerequisites
        run: |
          set -x
          time brew update --auto-update
          time brew install meson
