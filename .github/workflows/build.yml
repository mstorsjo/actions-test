name: Builds
on:
  push:

jobs:
  test:
    runs-on: windows-11-arm
    steps:
      - uses: actions/checkout@v4
      - name: Run test binary
        shell: cmd
        run: |
          ver
          exit1.exe
