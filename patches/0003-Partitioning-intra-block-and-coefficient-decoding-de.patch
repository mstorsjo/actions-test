From dd32cb9efacc7f7e511798d9a6af3e952d6afddc Mon Sep 17 00:00:00 2001
From: "Ronald S. Bultje" <rsbultje@gmail.com>
Date: Mon, 1 Sep 2025 09:39:53 -0400
Subject: [PATCH 3/3] Partitioning, intra block and coefficient decoding
 debugging helpers.

---
 av1/decoder/decodeframe.c |  70 ++++++++++++-
 av1/decoder/decodemv.c    | 209 ++++++++++++++++++++++++++++++++------
 av1/decoder/decoder.h     |  14 +++
 av1/decoder/decodetxb.c   | 192 ++++++++++++++++++++++++++++------
 4 files changed, 423 insertions(+), 62 deletions(-)

diff --git a/av1/decoder/decodeframe.c b/av1/decoder/decodeframe.c
index 999bead1fb..6684f0f083 100644
--- a/av1/decoder/decodeframe.c
+++ b/av1/decoder/decodeframe.c
@@ -233,6 +233,32 @@ static AOM_INLINE void read_coeffs_tx_intra_block(
     aom_usec_timer_start(&timer);
 #endif
     av1_read_coeffs_txb_facade(cm, dcb, r, plane, row, col, tx_size);
+#if DEBUG_BLOCK_INFO
+      MACROBLOCKD *const xd = &dcb->xd;
+      const int mi_row = xd->mi_row + row, mi_col = xd->mi_col + col;
+      eob_info *bob_eob_data;
+      const PLANE_TYPE plane_type = get_plane_type(plane);
+      const TX_TYPE tx_type =
+          av1_get_tx_type(xd, plane_type, row, col, tx_size,
+                          is_reduced_tx_set_used(cm, plane_type));
+      const int is_inter = is_inter_block(mbmi, xd->tree_type);
+      if (((mbmi->fsc_mode[xd->tree_type == CHROMA_PART] &&
+            get_primary_tx_type(tx_type) == IDTX && plane == PLANE_TYPE_Y) ||
+           use_inter_fsc(cm, plane, tx_type, is_inter))
+  #if CONFIG_FSC_RES_HLS
+          && cm->seq_params.enable_fsc_residual
+  #endif  // CONFIG_FSC_RES_HLS
+      ) {
+          bob_eob_data = dcb->bob_data[plane];
+      } else {
+          bob_eob_data = dcb->eob_data[plane];
+      }
+      DEBUG_BLOCK_printf("Post-y_cf_blk[tx=%dx%d,txtp=%d,eob=%d]: r=%d\n",
+                         tx_size_wide[tx_size], tx_size_high[tx_size],
+                         xd->tx_type_map[row * xd->tx_type_map_stride + col],
+                         (bob_eob_data + dcb->txb_offset[plane])->eob - 1,
+                         r->ec.rng);
+#endif
 #if TXCOEFF_TIMER
     aom_usec_timer_mark(&timer);
     const int64_t elapsed_time = aom_usec_timer_elapsed(&timer);
@@ -1587,12 +1613,14 @@ static AOM_INLINE void parse_decode_block(AV1Decoder *const pbi,
         max_aspect_ratio);
   }
 #endif  // CONFIG_MAX_PB_RATIO
+  AV1_COMMON *cm = &pbi->common;
+    DEBUG_BLOCK_printf("decode_b[y=%d,x=%d,bs=%dx%d]: r=%d\n",
+                       mi_row, mi_col, blk_w, blk_h, r->ec.rng);
   decode_mbmi_block(pbi, dcb, mi_row, mi_col, r, partition, bsize, parent,
                     index);
 
   av1_visit_palette(pbi, xd, r, av1_decode_palette_tokens);
 
-  AV1_COMMON *cm = &pbi->common;
   const int num_planes = av1_num_planes(cm);
   MB_MODE_INFO *mbmi = xd->mi[0];
   int inter_block_tx = is_inter_block(mbmi, xd->tree_type) ||
@@ -1620,6 +1648,10 @@ static AOM_INLINE void parse_decode_block(AV1Decoder *const pbi,
           read_tx_size(xd, cm->features.tx_mode, inter_block_tx,
                        !mbmi->skip_txfm[xd->tree_type == CHROMA_PART], r);
     }
+#if DEBUG_BLOCK_INFO
+      if (bsize != BLOCK_4X4)
+          DEBUG_BLOCK_printf("Post-tx: r=%d\n", r->ec.rng);
+#endif
   }
 
   if (cm->delta_q_info.delta_q_present_flag) {
@@ -2025,6 +2057,10 @@ static AOM_INLINE void decode_partition(
     return;
   }
 
+    DEBUG_BLOCK_printf("decode_sb[y=%d,x=%d,bs=%dx%d]: r=%d\n",
+                       mi_row, mi_col, 4 * mi_size_wide[bsize],
+                       4 * mi_size_high[bsize], reader->ec.rng);
+
   // parse_decode_flag takes the following values :
   // 01 - do parse only
   // 10 - do decode only
@@ -2105,6 +2141,8 @@ static AOM_INLINE void decode_partition(
             loop_restoration_read_sb_coeffs(cm, xd, reader, plane, runit_idx);
           }
         }
+          printf("decode_partition|post-rst[p=%d]: r=%d\n",
+                 plane, reader->ec.rng);
       }
     }
 
@@ -2135,6 +2173,25 @@ static AOM_INLINE void decode_partition(
                     ? PARTITION_NONE
                     : read_partition(cm, xd, mi_row, mi_col, reader, has_rows,
                                      has_cols, ptree, ptree_luma, bsize);
+#if DEBUG_BLOCK_INFO
+          static const char *const names[] = {
+              [PARTITION_NONE]="none",
+              [PARTITION_HORZ_4A]="h4a",
+              [PARTITION_HORZ_4B]="h4b",
+              [PARTITION_VERT_4A]="v4a",
+              [PARTITION_VERT_4B]="v4b",
+              [PARTITION_SPLIT]="s",
+              [PARTITION_HORZ]="h",
+              [PARTITION_VERT]="v",
+              [PARTITION_HORZ_3]="h3",
+              [PARTITION_VERT_3]="v3",
+          };
+          // FIXME add xd->tree_type here
+          DEBUG_BLOCK_printf("read_partition[y=%d,x=%d,bs=%dx%d,bp=%d|%s]: r=%d\n",
+                             mi_row, mi_col, 4 * mi_size_wide[bsize],
+                             4 * mi_size_high[bsize], partition,
+                             names[partition], reader->ec.rng);
+#endif
 #if CONFIG_SDP_CFL_LATENCY_FIX
     ptree->is_cfl_allowed_for_this_chroma_partition |=
         is_cfl_allowed_for_sdp(cm, xd, ptree_luma, partition, bsize);
@@ -5133,6 +5190,17 @@ static const uint8_t *decode_tiles(AV1Decoder *pbi, const uint8_t *data,
       td->dcb.xd.tile_ctx = &tile_data->tctx;
 
       // decode tile
+#if DEBUG_BLOCK_INFO
+        const int mi_row = row, mi_col = col;
+        TileInfo tile_info;
+        av1_tile_set_row(&tile_info, cm, tile_row);
+        av1_tile_set_col(&tile_info, cm, tile_col);
+        DEBUG_BLOCK_printf("decode_tile[tilerow=%d/col=%d,y=%d-%d,x=%d-%d,size=%td]: r=%d\n",
+                           tile_row, tile_col,
+                           row, tile_info.mi_row_end,
+                           col, tile_info.mi_col_end,
+                           tile_bs_buf->size, td->bit_reader->ec.rng);
+#endif
       decode_tile(pbi, td, row, col);
       aom_merge_corrupted_flag(&pbi->dcb.corrupted, td->dcb.corrupted);
       if (pbi->dcb.corrupted)
diff --git a/av1/decoder/decodemv.c b/av1/decoder/decodemv.c
index 47abeeb4ef..0e27e9f84f 100644
--- a/av1/decoder/decodemv.c
+++ b/av1/decoder/decodemv.c
@@ -72,6 +72,10 @@ static void read_gdf(AV1_COMMON *cm, aom_reader *r, MACROBLOCKD *const xd) {
       if (blk_idx >= 0) {
         cm->gdf_info.gdf_block_flags[blk_idx] = aom_read_symbol(
             r, xd->tile_ctx->gdf_cdf, 2, ACCT_INFO("gdf_onoff"));
+          DEBUG_BLOCK_printf("Post-gdf[y=%d,x=%d,gdf=%d]: r=%d\n",
+                             mi_row, mi_col,
+                             cm->gdf_info.gdf_block_flags[blk_idx],
+                             r->ec.rng);
       }
     }
   }
@@ -130,6 +134,8 @@ static void read_cdef(AV1_COMMON *cm, aom_reader *r, MACROBLOCKD *const xd) {
               1;
         }
       }
+        DEBUG_BLOCK_printf("Post-cdef_idx[%d]: r=%d\n",
+                           mbmi->cdef_strength, r->ec.rng);
     }
     xd->cdef_transmitted[index] = true;
   } else {
@@ -191,6 +197,8 @@ static void read_ccso(AV1_COMMON *cm, aom_reader *r, MACROBLOCKD *const xd) {
       const int ccso_ctx = av1_get_ccso_context(xd, 0);
       blk_idc = aom_read_symbol(r, xd->tile_ctx->ccso_cdf[0][ccso_ctx], 2,
                                 ACCT_INFO("blk_idc"));
+      DEBUG_BLOCK_printf("Post-ccso[pl=y,%d]: r=%d\n",
+                         blk_idc, r->ec.rng);
     } else {
       CcsoInfo *ref_frame_ccso_info =
           &get_ref_frame_buf(cm, cm->ccso_info.ccso_ref_idx[0])->ccso_info;
@@ -226,6 +234,8 @@ static void read_ccso(AV1_COMMON *cm, aom_reader *r, MACROBLOCKD *const xd) {
       const int ccso_ctx = av1_get_ccso_context(xd, 1);
       blk_idc = aom_read_symbol(r, xd->tile_ctx->ccso_cdf[1][ccso_ctx], 2,
                                 ACCT_INFO("blk_idc"));
+      DEBUG_BLOCK_printf("Post-ccso[pl=u,%d]: r=%d\n",
+                         blk_idc, r->ec.rng);
     } else {
       CcsoInfo *ref_frame_ccso_info =
           &get_ref_frame_buf(cm, cm->ccso_info.ccso_ref_idx[1])->ccso_info;
@@ -261,6 +271,8 @@ static void read_ccso(AV1_COMMON *cm, aom_reader *r, MACROBLOCKD *const xd) {
       const int ccso_ctx = av1_get_ccso_context(xd, 2);
       blk_idc = aom_read_symbol(r, xd->tile_ctx->ccso_cdf[2][ccso_ctx], 2,
                                 ACCT_INFO("blk_idc"));
+      DEBUG_BLOCK_printf("Post-ccso[pl=v,%d]: r=%d\n",
+                         blk_idc, r->ec.rng);
     } else {
       CcsoInfo *ref_frame_ccso_info =
           &get_ref_frame_buf(cm, cm->ccso_info.ccso_ref_idx[2])->ccso_info;
@@ -339,17 +351,32 @@ static int read_delta_lflevel(const AV1_COMMON *const cm, aom_reader *r,
   return reduced_delta_lflevel;
 }
 
-static uint8_t read_mrl_index(FRAME_CONTEXT *ec_ctx, aom_reader *r,
+static uint8_t read_mrl_index(
+#if DEBUG_BLOCK_INFO
+                              const AV1_COMMON *const cm,
+                              const MACROBLOCKD *xd,
+#endif
+                              FRAME_CONTEXT *ec_ctx, aom_reader *r,
                               const MB_MODE_INFO *neighbor0,
                               const MB_MODE_INFO *neighbor1) {
   int ctx = get_mrl_index_ctx(neighbor0, neighbor1);
   aom_cdf_prob *mrl_cdf = ec_ctx->mrl_index_cdf[ctx];
   const uint8_t mrl_index =
       aom_read_symbol(r, mrl_cdf, MRL_LINE_NUMBER, ACCT_INFO());
+#if DEBUG_BLOCK_INFO
+    int mi_row = xd->mi_row, mi_col = xd->mi_col;
+    DEBUG_BLOCK_printf("Post-mrl_index[ctx=%d,%d]: r=%d\n",
+                       ctx, mrl_index, r->ec.rng);
+#endif
   return mrl_index;
 }
 
-static bool read_multi_line_mrl(FRAME_CONTEXT *ec_ctx, aom_reader *r,
+static bool read_multi_line_mrl(
+#if DEBUG_BLOCK_INFO
+                                const AV1_COMMON *const cm,
+                                const MACROBLOCKD *xd,
+#endif
+                                FRAME_CONTEXT *ec_ctx, aom_reader *r,
                                 const MB_MODE_INFO *neighbor0,
                                 const MB_MODE_INFO *neighbor1) {
   int multi_line_mrl_ctx = get_multi_line_mrl_index_ctx(neighbor0, neighbor1);
@@ -357,6 +384,11 @@ static bool read_multi_line_mrl(FRAME_CONTEXT *ec_ctx, aom_reader *r,
       ec_ctx->multi_line_mrl_cdf[multi_line_mrl_ctx];
   const bool multi_line_mrl =
       aom_read_symbol(r, multi_line_mrl_cdf, 2, ACCT_INFO());
+#if DEBUG_BLOCK_INFO
+    int mi_row = xd->mi_row, mi_col = xd->mi_col;
+    DEBUG_BLOCK_printf("Post-multi_line_mrl[ctx=%d,%d]: r=%d\n",
+                       multi_line_mrl_ctx, multi_line_mrl, r->ec.rng);
+#endif
   return multi_line_mrl;
 }
 
@@ -947,6 +979,11 @@ static int read_segment_id(AV1_COMMON *const cm, const MACROBLOCKD *const xd,
   const int segment_id =
       av1_neg_deinterleave(coded_id, pred, seg->last_active_segid + 1);
 
+#if DEBUG_BLOCK_INFO
+    const int mi_row = xd->mi_row, mi_col = xd->mi_col;
+    DEBUG_BLOCK_printf("Post-segid[%d]: r=%d\n",
+                       segment_id, r->ec.rng);
+#endif
   if (segment_id < 0 || segment_id > seg->last_active_segid) {
     aom_internal_error(xd->error_info, AOM_CODEC_CORRUPT_FRAME,
                        "Corrupted segment_ids");
@@ -1214,6 +1251,9 @@ static void read_palette_mode_info(AV1_COMMON *const cm, MACROBLOCKD *const xd,
   const BLOCK_SIZE bsize = mbmi->sb_type[xd->tree_type == CHROMA_PART];
   assert(av1_allow_palette(PLANE_TYPE_Y,
                            cm->features.allow_screen_content_tools, bsize));
+#if DEBUG_BLOCK_INFO
+    int mi_row = xd->mi_row, mi_col = xd->mi_col;
+#endif
 #if CONFIG_PALETTE_CTX_REDUCTION
   (void)bsize;
 #endif  // CONFIG_PALETTE_CTX_REDUCTION
@@ -1246,6 +1286,7 @@ static void read_palette_mode_info(AV1_COMMON *const cm, MACROBLOCKD *const xd,
 #endif  // CONFIG_PALETTE_CTX_REDUCTION
       read_palette_colors_y(xd, cm->seq_params.bit_depth, pmi, r);
     }
+      DEBUG_BLOCK_printf("Post-ypal: r=%d\n", r->ec.rng);
   }
 #if !CONFIG_DISABLE_PALC
   const int num_planes = av1_num_planes(cm);
@@ -1276,6 +1317,7 @@ static void read_palette_mode_info(AV1_COMMON *const cm, MACROBLOCKD *const xd,
 #endif  // CONFIG_PALETTE_CTX_REDUCTION
       read_palette_colors_uv(xd, cm->seq_params.bit_depth, pmi, r);
     }
+      DEBUG_BLOCK_printf("Post-uvpal: r=%d\n", r->ec.rng);
   }
 #endif  // !CONFIG_DISABLE_PALC
 }
@@ -1303,6 +1345,11 @@ static void read_intra_dip_mode_info(const AV1_COMMON *const cm,
       mbmi->intra_dip_mode +=
           aom_read_symbol(r, mode_cdf, n_modes, ACCT_INFO("intra_dip_mode_n6"));
     }
+#if DEBUG_BLOCK_INFO
+      int mi_row = xd->mi_row, mi_col = xd->mi_col;
+      DEBUG_BLOCK_printf("Post-dip[ctx=%d,%d]: r=%d\n",
+                         ctx, mbmi->use_intra_dip, r->ec.rng);
+#endif
   }
 }
 
@@ -1339,7 +1386,6 @@ void av1_read_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd, int blk_row,
   }
 
   const int inter_block = is_inter_block(mbmi, xd->tree_type);
-
   if (get_ext_tx_types(tx_size, inter_block, cm->features.reduced_tx_set_used) >
       1) {
     const TxSetType tx_set_type = av1_get_ext_tx_set_type(
@@ -1501,7 +1547,7 @@ void av1_read_cctx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
 }
 
 // This function reads a 'secondary tx set' from the bitstream
-static void read_secondary_tx_set(MACROBLOCKD *xd, FRAME_CONTEXT *ec_ctx,
+static TX_TYPE read_secondary_tx_set(MACROBLOCKD *xd, FRAME_CONTEXT *ec_ctx,
                                   aom_reader *r, MB_MODE_INFO *mbmi,
                                   TX_SIZE tx_size, TX_TYPE *tx_type) {
   const int inter_block = is_inter_block(mbmi, xd->tree_type);
@@ -1530,6 +1576,7 @@ static void read_secondary_tx_set(MACROBLOCKD *xd, FRAME_CONTEXT *ec_ctx,
   }
   if (get_primary_tx_type(*tx_type) == ADST_ADST) stx_set_flag += IST_DIR_SIZE;
   set_secondary_tx_set(tx_type, stx_set_flag);
+  return stx_set_flag;
 }
 
 void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
@@ -1548,6 +1595,11 @@ void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
   if (xd->lossless[mbmi->segment_id]) return;
 
   const int inter_block = is_inter_block(mbmi, xd->tree_type);
+#if DEBUG_BLOCK_INFO
+    TX_TYPE stx_set = DC_PRED;
+    const int mi_row = xd->mi_row + blk_row, mi_col = xd->mi_col + blk_col;
+    const int plane = 0 /* stx implies Y */;
+#endif
   if (get_ext_tx_types(tx_size, inter_block, cm->features.reduced_tx_set_used) >
       1) {
     FRAME_CONTEXT *ec_ctx = xd->tile_ctx;
@@ -1558,7 +1610,10 @@ void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
                           STX_TYPES, ACCT_INFO("stx_flag"));
       *tx_type |= (stx_flag << PRIMARY_TX_BITS);
       if (stx_flag > 0)
-        read_secondary_tx_set(xd, ec_ctx, r, mbmi, tx_size, tx_type);
+#if DEBUG_BLOCK_INFO
+        stx_set =
+#endif
+                  read_secondary_tx_set(xd, ec_ctx, r, mbmi, tx_size, tx_type);
 #if STX_SYNTAX_DEBUG
       const int sb_size =
           (tx_size_wide[tx_size] >= 8 && tx_size_high[tx_size] >= 8) ? 8 : 4;
@@ -1569,6 +1624,8 @@ void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
               tx_size_high[tx_size], *eob, get_primary_tx_type(*tx_type),
               stx_flag, get_secondary_tx_set(*tx_type));
 #endif  // STX_SYNTAX_DEBUG
+        DEBUG_CF_printf("Post-stx[type=%d,set=%d]: r=%d\n",
+                        stx_flag, stx_set, r->ec.rng);
     }
   } else {
     FRAME_CONTEXT *ec_ctx = xd->tile_ctx;
@@ -1579,7 +1636,10 @@ void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
                           STX_TYPES, ACCT_INFO("stx_flag"));
       *tx_type |= (stx_flag << PRIMARY_TX_BITS);
       if (stx_flag > 0)
-        read_secondary_tx_set(xd, ec_ctx, r, mbmi, tx_size, tx_type);
+#if DEBUG_BLOCK_INFO
+        stx_set =
+#endif
+                  read_secondary_tx_set(xd, ec_ctx, r, mbmi, tx_size, tx_type);
 #if STX_SYNTAX_DEBUG
       const int sb_size =
           (tx_size_wide[tx_size] >= 8 && tx_size_high[tx_size] >= 8) ? 8 : 4;
@@ -1590,6 +1650,8 @@ void av1_read_sec_tx_type(const AV1_COMMON *const cm, MACROBLOCKD *xd,
               tx_size_high[tx_size], *eob, get_primary_tx_type(*tx_type),
               stx_flag, get_secondary_tx_set(*tx_type));
 #endif  // STX_SYNTAX_DEBUG
+        DEBUG_CF_printf("Post-stx[type=%d,set=%d]: r=%d\n",
+                        stx_flag, stx_set, r->ec.rng);
     }
   }
 }
@@ -1784,38 +1846,46 @@ static void read_delta_q_params(AV1_COMMON *const cm, MACROBLOCKD *const xd,
 
   if (delta_q_info->delta_q_present_flag) {
     MB_MODE_INFO *const mbmi = xd->mi[0];
-    xd->current_base_qindex +=
-        read_delta_qindex(cm, xd, r, mbmi) * delta_q_info->delta_q_res;
+    const int delta_q_val = read_delta_qindex(cm, xd, r, mbmi);
+    xd->current_base_qindex += delta_q_val * delta_q_info->delta_q_res;
     /* Normative: Clamp to [1,MAXQ] to not interfere with lossless mode */
     xd->current_base_qindex =
         clamp(xd->current_base_qindex, 1,
               cm->seq_params.bit_depth == AOM_BITS_8    ? MAXQ_8_BITS
               : cm->seq_params.bit_depth == AOM_BITS_10 ? MAXQ_10_BITS
                                                         : MAXQ);
-    FRAME_CONTEXT *const ec_ctx = xd->tile_ctx;
-    if (delta_q_info->delta_lf_present_flag) {
       const int mi_row = xd->mi_row;
       const int mi_col = xd->mi_col;
+#if DEBUG_BLOCK_INFO
+      DEBUG_BLOCK_printf("Post-delta_q[%d->%d]: r=%d\n",
+                         delta_q_val, xd->current_base_qindex, r->ec.rng);
+#endif
+    FRAME_CONTEXT *const ec_ctx = xd->tile_ctx;
+    if (delta_q_info->delta_lf_present_flag) {
       if (delta_q_info->delta_lf_multi) {
         const int frame_lf_count =
             av1_num_planes(cm) > 1 ? FRAME_LF_COUNT : FRAME_LF_COUNT - 2;
         for (int lf_id = 0; lf_id < frame_lf_count; ++lf_id) {
-          const int tmp_lvl =
-              xd->delta_lf[lf_id] +
+          const int delta_lf_val =
               read_delta_lflevel(cm, r, ec_ctx->delta_lf_multi_cdf[lf_id], mbmi,
-                                 mi_col, mi_row, xd->tree_type) *
+                                 mi_col, mi_row, xd->tree_type);
+          const int tmp_lvl = xd->delta_lf[lf_id] + delta_lf_val *
                   delta_q_info->delta_lf_res;
           mbmi->delta_lf[lf_id] = xd->delta_lf[lf_id] =
               clamp(tmp_lvl, -MAX_LOOP_FILTER, MAX_LOOP_FILTER);
+          DEBUG_BLOCK_printf("Post-delta_lf[%d:%d->%d]: r=%d\n",
+                             lf_id, delta_lf_val, mbmi->delta_lf[lf_id], r->ec.rng);
         }
       } else {
-        const int tmp_lvl =
-            xd->delta_lf_from_base +
+        const int delta_lf_val =
             read_delta_lflevel(cm, r, ec_ctx->delta_lf_cdf, mbmi, mi_col,
-                               mi_row, xd->tree_type) *
+                               mi_row, xd->tree_type);
+        const int tmp_lvl = xd->delta_lf_from_base + delta_lf_val *
                 delta_q_info->delta_lf_res;
         mbmi->delta_lf_from_base = xd->delta_lf_from_base =
             clamp(tmp_lvl, -MAX_LOOP_FILTER, MAX_LOOP_FILTER);
+          DEBUG_BLOCK_printf("Post-delta_lf[%d->%d]: r=%d\n",
+                             delta_lf_val, mbmi->delta_lf_from_base, r->ec.rng);
       }
     }
   }
@@ -1823,11 +1893,15 @@ static void read_delta_q_params(AV1_COMMON *const cm, MACROBLOCKD *const xd,
 
 // read mode set index and mode index in set for y component,
 // and map it to y mode and delta angle
-static void read_intra_luma_mode(MACROBLOCKD *const xd, aom_reader *r) {
+static void read_intra_luma_mode(
+#if DEBUG_BLOCK_INFO
+                                 AV1_COMMON *const cm,
+#endif
+                                 MACROBLOCKD *const xd, aom_reader *r) {
   FRAME_CONTEXT *ec_ctx = xd->tile_ctx;
   MB_MODE_INFO *const mbmi = xd->mi[0];
   uint8_t mode_idx = 0;
-  const int context = get_y_mode_idx_ctx(xd);
+  int context = get_y_mode_idx_ctx(xd);
   int mode_set_index =
       aom_read_symbol(r, ec_ctx->y_mode_set_cdf, INTRA_MODE_SETS,
                       ACCT_INFO("mode_set_index", "y_mode_set_cdf"));
@@ -1836,11 +1910,12 @@ static void read_intra_luma_mode(MACROBLOCKD *const xd, aom_reader *r) {
     mode_idx = aom_read_symbol(r, ec_ctx->y_mode_idx_cdf[context],
                                LUMA_INTRA_MODE_INDEX_COUNT,
                                ACCT_INFO("mode_idx", "y_mode_idx_cdf"));
-    if (mode_idx == (LUMA_INTRA_MODE_INDEX_COUNT - 1))
+    if (mode_idx == (LUMA_INTRA_MODE_INDEX_COUNT - 1)) {
       mode_idx +=
           aom_read_symbol(r, ec_ctx->y_mode_idx_offset_cdf[context],
                           LUMA_INTRA_MODE_OFFSET_COUNT,
                           ACCT_INFO("mode_idx", "y_mode_idx_offset_cdf"));
+    }
 #else
     mode_idx =
         aom_read_symbol(r, ec_ctx->y_mode_idx_cdf_0[context], FIRST_MODE_COUNT,
@@ -1850,6 +1925,9 @@ static void read_intra_luma_mode(MACROBLOCKD *const xd, aom_reader *r) {
     mode_idx = FIRST_MODE_COUNT + (mode_set_index - 1) * SECOND_MODE_COUNT +
 #if CONFIG_CTX_Y_SECOND_MODE || CONFIG_REDUCE_SYMBOL_SIZE
                aom_read_literal(r, 4, ACCT_INFO("mode_idx"));
+#if DEBUG_BLOCK_INFO
+      context = -1;
+#endif
 #else
                aom_read_symbol(r, ec_ctx->y_mode_idx_cdf_1[context],
                                SECOND_MODE_COUNT,
@@ -1863,6 +1941,13 @@ static void read_intra_luma_mode(MACROBLOCKD *const xd, aom_reader *r) {
   mbmi->y_mode_idx = mode_idx;
   if (mbmi->joint_y_mode_delta_angle < NON_DIRECTIONAL_MODES_COUNT)
     assert(mbmi->joint_y_mode_delta_angle == mbmi->y_mode_idx);
+#if DEBUG_BLOCK_INFO
+    const int mi_row = xd->mi_row;
+    const int mi_col = xd->mi_col;
+    DEBUG_BLOCK_printf("Post-intra_y_mode[set=%d,idx=%d,ctx=%d,mode=%d,angle=%d]: r=%d\n",
+           mode_set_index, mode_idx, context,
+           mbmi->mode, mbmi->angle_delta[PLANE_TYPE_Y], r->ec.rng);
+#endif
 }
 
 // Read mode index for uv component and map it to uv mode and delta angle.
@@ -1919,6 +2004,11 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
 
   FRAME_CONTEXT *ec_ctx = xd->tile_ctx;
 
+#if DEBUG_BLOCK_INFO
+    const int mi_row = xd->mi_row;
+    const int mi_col = xd->mi_col;
+#endif
+
   if (seg->segid_preskip && xd->tree_type != CHROMA_PART)
     mbmi->segment_id = read_intra_segment_id(cm, xd, bsize, r, 0);
 
@@ -1934,10 +2024,16 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
     mbmi->use_intrabc[xd->tree_type == CHROMA_PART] =
         aom_read_symbol(r, ec_ctx->intrabc_cdf[intrabc_ctx], 2,
                         ACCT_INFO("use_intrabc", "chroma"));
+      DEBUG_BLOCK_printf("Post-is_intrabc[%d]: r=%d\n",
+                         mbmi->use_intrabc[xd->tree_type == CHROMA_PART],
+                         r->ec.rng);
   }
   if (is_intrabc_block(mbmi, xd->tree_type)) {
     mbmi->skip_txfm[xd->tree_type == CHROMA_PART] =
         read_skip_txfm(cm, xd, mbmi->segment_id, r);
+      DEBUG_BLOCK_printf("Post-skip_txfm[%d]: r=%d\n",
+                         mbmi->skip_txfm[xd->tree_type == CHROMA_PART],
+                         r->ec.rng);
   } else {
     // Segment SEG_LVL_SKIP should be disabled for intra prediction
     if (segfeature_active(&cm->seg, mbmi->segment_id, SEG_LVL_SKIP)) {
@@ -1985,7 +2081,11 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
     if (xd->lossless[mbmi->segment_id]) {
       mbmi->use_dpcm_y = read_dpcm_mode(ec_ctx, r);
       if (mbmi->use_dpcm_y == 0) {
-        read_intra_luma_mode(xd, r);
+        read_intra_luma_mode(
+#if DEBUG_BLOCK_INFO
+                             cm,
+#endif
+                             xd, r);
       } else {
         mbmi->dpcm_mode_y = read_dpcm_vert_horz_mode(ec_ctx, r);
         if (mbmi->dpcm_mode_y == 0) {
@@ -2001,11 +2101,23 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
     } else {
       mbmi->use_dpcm_y = 0;
       mbmi->dpcm_mode_y = 0;
-      read_intra_luma_mode(xd, r);
+      read_intra_luma_mode(
+#if DEBUG_BLOCK_INFO
+                           cm,
+#endif
+                           xd, r);
     }
     if (allow_fsc_intra(cm, bsize, mbmi)) {
       aom_cdf_prob *fsc_cdf = get_fsc_mode_cdf(xd, bsize, 1);
+#if DEBUG_BLOCK_INFO
+        const int ctx = get_fsc_mode_ctx(xd, 1);
+        const uint8_t fsc_size_group = fsc_bsize_groups[bsize];
+#endif
       mbmi->fsc_mode[xd->tree_type == CHROMA_PART] = read_fsc_mode(r, fsc_cdf);
+        DEBUG_BLOCK_printf("Post-fsc[ctx=%d|%d,%d]: r=%d\n",
+                           ctx, fsc_size_group,
+                           mbmi->fsc_mode[xd->tree_type == CHROMA_PART],
+                           r->ec.rng);
     } else {
       mbmi->fsc_mode[xd->tree_type == CHROMA_PART] = 0;
     }
@@ -2014,10 +2126,17 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
       if (mbmi->use_dpcm_y == 0) {
         mbmi->mrl_index =
             (cm->seq_params.enable_mrls && av1_is_directional_mode(mbmi->mode))
-                ? read_mrl_index(ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
+                ? read_mrl_index(
+#if DEBUG_BLOCK_INFO
+                                 cm, xd,
+#endif
+                                 ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
                 : 0;
         if (mbmi->mrl_index) {
           mbmi->multi_line_mrl = read_multi_line_mrl(
+#if DEBUG_BLOCK_INFO
+              cm, xd,
+#endif
               ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
         } else {
           mbmi->multi_line_mrl = 0;
@@ -2029,11 +2148,19 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
     } else {
       mbmi->mrl_index =
           (cm->seq_params.enable_mrls && av1_is_directional_mode(mbmi->mode))
-              ? read_mrl_index(ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
+              ? read_mrl_index(
+#if DEBUG_BLOCK_INFO
+                               cm, xd,
+#endif
+                               ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
               : 0;
       if (mbmi->mrl_index) {
         mbmi->multi_line_mrl =
-            read_multi_line_mrl(ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
+            read_multi_line_mrl(
+#if DEBUG_BLOCK_INFO
+                                cm, xd,
+#endif
+                                ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
       } else {
         mbmi->multi_line_mrl = 0;
       }
@@ -2117,6 +2244,7 @@ static void read_intra_frame_mode_info(AV1_COMMON *const cm,
           mbmi->cfl_alpha_idx =
               read_cfl_alphas(ec_ctx, r, &mbmi->cfl_alpha_signs);
       }
+            DEBUG_BLOCK_printf("Post-uvmode: r=%d\n", r->ec.rng);
     } else {
       // Avoid decoding angle_info if there is is no chroma prediction
       mbmi->uv_mode = UV_DC_PRED;
@@ -2775,7 +2903,11 @@ static void read_intra_block_mode_info(AV1_COMMON *const cm,
     if (xd->lossless[mbmi->segment_id]) {
       mbmi->use_dpcm_y = read_dpcm_mode(ec_ctx, r);
       if (mbmi->use_dpcm_y == 0) {
-        read_intra_luma_mode(xd, r);
+        read_intra_luma_mode(
+#if DEBUG_BLOCK_INFO
+                             cm,
+#endif
+                             xd, r);
       } else {
         mbmi->dpcm_mode_y = read_dpcm_vert_horz_mode(ec_ctx, r);
         if (mbmi->dpcm_mode_y == 0) {
@@ -2791,7 +2923,11 @@ static void read_intra_block_mode_info(AV1_COMMON *const cm,
     } else {
       mbmi->use_dpcm_y = 0;
       mbmi->dpcm_mode_y = 0;
-      read_intra_luma_mode(xd, r);
+      read_intra_luma_mode(
+#if DEBUG_BLOCK_INFO
+                           cm,
+#endif
+                           xd, r);
     }
   }
 
@@ -2810,10 +2946,17 @@ static void read_intra_block_mode_info(AV1_COMMON *const cm,
       if (mbmi->use_dpcm_y == 0) {
         mbmi->mrl_index =
             (cm->seq_params.enable_mrls && av1_is_directional_mode(mbmi->mode))
-                ? read_mrl_index(ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
+                ? read_mrl_index(
+#if DEBUG_BLOCK_INFO
+                                 cm, xd,
+#endif
+                                 ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
                 : 0;
         if (mbmi->mrl_index) {
           mbmi->multi_line_mrl = read_multi_line_mrl(
+#if DEBUG_BLOCK_INFO
+              cm, xd,
+#endif
               ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
         } else {
           mbmi->multi_line_mrl = 0;
@@ -2825,11 +2968,19 @@ static void read_intra_block_mode_info(AV1_COMMON *const cm,
     } else {
       mbmi->mrl_index =
           (cm->seq_params.enable_mrls && av1_is_directional_mode(mbmi->mode))
-              ? read_mrl_index(ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
+              ? read_mrl_index(
+#if DEBUG_BLOCK_INFO
+                               cm, xd,
+#endif
+                               ec_ctx, r, xd->neighbors[0], xd->neighbors[1])
               : 0;
       if (mbmi->mrl_index) {
         mbmi->multi_line_mrl =
-            read_multi_line_mrl(ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
+            read_multi_line_mrl(
+#if DEBUG_BLOCK_INFO
+                                cm, xd,
+#endif
+                                ec_ctx, r, xd->neighbors[0], xd->neighbors[1]);
       } else {
         mbmi->multi_line_mrl = 0;
       }
diff --git a/av1/decoder/decoder.h b/av1/decoder/decoder.h
index 2c8ad00d2c..d2e63efa9b 100644
--- a/av1/decoder/decoder.h
+++ b/av1/decoder/decoder.h
@@ -33,6 +33,20 @@
 #define DEBUG_OBU_HDR 0
 #define DEBUG_SEQ_HDR 0
 #define DEBUG_FRAME_HDR 0
+#define DEBUG_BLOCK_INFO 0
+#if DEBUG_BLOCK_INFO
+#define BLOCK_TO_DEBUG \
+    cm->current_frame.frame_number == 0 && \
+    mi_row >= 0 && mi_row < 16 && mi_col >= 0 && mi_col < 16
+#define DEBUG_BLOCK_printf(fmt...) \
+    if (BLOCK_TO_DEBUG) printf(fmt)
+#define DEBUG_CF_printf(fmt...) \
+    if (BLOCK_TO_DEBUG && plane == 0 && 0) printf(fmt)
+#else
+#define BLOCK_TO_DEBUG 0
+#define DEBUG_BLOCK_printf(fmt...)
+#define DEBUG_CF_printf(fmt...)
+#endif
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/av1/decoder/decodetxb.c b/av1/decoder/decodetxb.c
index 4f85230ef3..8c5b7e61be 100644
--- a/av1/decoder/decodetxb.c
+++ b/av1/decoder/decodetxb.c
@@ -117,6 +117,11 @@ static int read_adaptive_hr(MACROBLOCKD *xd, aom_reader *r, int ctx) {
 // Read high range part of coeff
 static INLINE int read_high_range(MACROBLOCKD *xd, aom_reader *r, int tcq_mode,
                                   int level, int lf, int *hr_avg
+#if DEBUG_BLOCK_INFO
+                                  , const AV1_COMMON *const cm,
+                                  const int mi_row,
+                                  const int mi_col, const int c
+#endif
 #if CONFIG_COEFF_BR_LF_UV_BYPASS
                                   ,
                                   int plane
@@ -135,6 +140,8 @@ static INLINE int read_high_range(MACROBLOCKD *xd, aom_reader *r, int tcq_mode,
   if (use_hr) {
     int hr = read_adaptive_hr(xd, r, hr_level_avg);
     level += hr << (tcq_mode ? 1 : 0);
+      DEBUG_CF_printf("Post-residual[pos=%d,%d->%d]: r=%d\n",
+                      c, hr, level, r->ec.rng);
     *hr_avg = (hr_level_avg + hr) >> 1;
   }
   return level;
@@ -176,7 +183,12 @@ static INLINE void read_coeffs_reverse_2d(
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
     br_cdf_arr br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-    base_cdf_arr base_uv_cdf, br_cdf_arr br_uv_cdf, int *state) {
+    base_cdf_arr base_uv_cdf, br_cdf_arr br_uv_cdf, int *state
+#if DEBUG_BLOCK_INFO
+    , const AV1_COMMON *const cm, const TX_SIZE txs_ctx,
+    const int mi_row, const int mi_col
+#endif
+    ) {
   for (int c = end_si; c >= start_si; --c) {
     const int pos = scan[c];
     int level = 0;
@@ -185,51 +197,59 @@ static INLINE void read_coeffs_reverse_2d(
     int limits = get_lf_limits(row, col, 0, plane);
     int q_i = tcq_quant(*state);
     if (plane > 0) {
+        int br_ctx = -1, coeff_ctx;
       if (limits) {
-        const int coeff_ctx =
+        coeff_ctx =
             get_lower_levels_ctx_lf_2d_chroma(levels, pos, bwl, plane);
         level +=
             aom_read_symbol(r, base_lf_uv_cdf[coeff_ctx][q_i], LF_BASE_SYMBOLS,
                             ACCT_INFO("level", "base_lf_uv_cdf"));
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_lf_ctx_2d_chroma(levels, pos, bwl);
+          br_ctx = get_br_lf_ctx_2d_chroma(levels, pos, bwl);
           aom_cdf_prob *cdf = br_lf_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
       } else {
-        const int coeff_ctx =
+        coeff_ctx =
             get_lower_levels_ctx_2d_chroma(levels, pos, bwl, plane);
         level += aom_read_symbol(r, base_uv_cdf[coeff_ctx][q_i], 4,
                                  ACCT_INFO("level", "base_uv_cdf"));
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx_2d_chroma(levels, pos, bwl);
+          br_ctx = get_br_ctx_2d_chroma(levels, pos, bwl);
           aom_cdf_prob *cdf = br_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-tok[pos=%d,ctx=%d|%d|%d,freq=%s,plane=uv,%d]: r=%d\n",
+                        c, coeff_ctx, q_i, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     } else {
+        int br_ctx = -1, coeff_ctx;
       if (limits) {
-        const int coeff_ctx = get_lower_levels_ctx_lf_2d(levels, pos, bwl);
+        coeff_ctx = get_lower_levels_ctx_lf_2d(levels, pos, bwl);
         level +=
             aom_read_symbol(r, base_lf_cdf[coeff_ctx][q_i], LF_BASE_SYMBOLS,
                             ACCT_INFO("level", "base_lf_cdf"));
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_lf_ctx_2d(levels, pos, bwl);
+          br_ctx = get_br_lf_ctx_2d(levels, pos, bwl);
           aom_cdf_prob *cdf = br_lf_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       } else {
-        const int coeff_ctx = get_lower_levels_ctx_2d(levels, pos, bwl, plane);
+        coeff_ctx = get_lower_levels_ctx_2d(levels, pos, bwl, plane);
         level += aom_read_symbol(r, base_cdf[coeff_ctx][q_i], 4,
                                  ACCT_INFO("level", "base_cdf"));
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx_2d(levels, pos, bwl);
+          br_ctx = get_br_ctx_2d(levels, pos, bwl);
           aom_cdf_prob *cdf = br_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-tok[pos=%d,ctx=%d|%d|%d|%d,freq=%s,plane=y,%d]: r=%d\n",
+                        c, txs_ctx, coeff_ctx, q_i, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     }
     levels[get_padded_idx(pos, bwl)] = level;
     *state = tcq_next_state(*state, level);
@@ -244,7 +264,12 @@ static INLINE void read_coeffs_reverse(
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
     br_cdf_arr br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-    base_cdf_arr base_uv_cdf, br_cdf_arr br_uv_cdf, int *state) {
+    base_cdf_arr base_uv_cdf, br_cdf_arr br_uv_cdf, int *state
+#if DEBUG_BLOCK_INFO
+    , const AV1_COMMON *const cm, const TX_SIZE txs_ctx,
+    const int mi_row, const int mi_col
+#endif
+    ) {
   for (int c = end_si; c >= start_si; --c) {
     const int pos = scan[c];
     int level = 0;
@@ -253,54 +278,63 @@ static INLINE void read_coeffs_reverse(
     const int col = pos - (row << bwl);
     int limits = get_lf_limits(row, col, tx_class, plane);
     if (plane > 0) {
+        int br_ctx = -1,coeff_ctx;
       if (limits) {
-        const int coeff_ctx =
+          coeff_ctx =
             get_lower_levels_lf_ctx_chroma(levels, pos, bwl, tx_class, plane);
         level +=
             aom_read_symbol(r, base_lf_uv_cdf[coeff_ctx][q_i], LF_BASE_SYMBOLS,
                             ACCT_INFO("level", "base_lf_uv_cdf"));
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_lf_ctx_chroma(levels, pos, bwl, tx_class);
+          br_ctx = get_br_lf_ctx_chroma(levels, pos, bwl, tx_class);
           aom_cdf_prob *cdf = br_lf_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
       } else {
-        const int coeff_ctx =
+        coeff_ctx =
             get_lower_levels_ctx_chroma(levels, pos, bwl, tx_class, plane);
         level += aom_read_symbol(r, base_uv_cdf[coeff_ctx][q_i], 4,
                                  ACCT_INFO("level", "base_uv_cdf"));
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx_chroma(levels, pos, bwl, tx_class);
+          br_ctx = get_br_ctx_chroma(levels, pos, bwl, tx_class);
           aom_cdf_prob *cdf = br_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-lo_tok[pos=%d,ctx=%d|%d|%d,freq=%s,plane=uv,%d]: r=%d\n",
+                        c, coeff_ctx, q_i, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     } else {
+        int br_ctx = -1, coeff_ctx;
       if (limits) {
-        const int coeff_ctx =
+        coeff_ctx =
             get_lower_levels_lf_ctx(levels, pos, bwl, tx_class);
         level +=
             aom_read_symbol(r, base_lf_cdf[coeff_ctx][q_i], LF_BASE_SYMBOLS,
                             ACCT_INFO("level", "base_lf_cdf"));
 
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_lf_ctx(levels, pos, bwl, tx_class);
+          br_ctx = get_br_lf_ctx(levels, pos, bwl, tx_class);
           aom_cdf_prob *cdf = br_lf_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       } else {
-        const int coeff_ctx =
+        coeff_ctx =
             get_lower_levels_ctx(levels, pos, bwl, tx_class, plane);
         level += aom_read_symbol(r, base_cdf[coeff_ctx][q_i], 4,
                                  ACCT_INFO("level", "base_cdf"));
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx(levels, pos, bwl, tx_class);
+          br_ctx = get_br_ctx(levels, pos, bwl, tx_class);
           aom_cdf_prob *cdf = br_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-%stok[pos=%d,ctx=%d|%d|%d|%d,freq=%s,plane=y,%d]: r=%d\n",
+                        c == 0 ? "dc_" : "",
+                        c, txs_ctx, coeff_ctx, q_i, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     }
     levels[get_padded_idx(pos, bwl)] = level;
     *state = tcq_next_state(*state, level);
@@ -311,18 +345,28 @@ static INLINE void read_coeffs_forward_2d(aom_reader *r, int start_si,
                                           int end_si, const int16_t *scan,
                                           int bwl, uint8_t *levels,
                                           base_fsc_cdf_arr base_cdf,
-                                          br_cdf_arr br_cdf) {
+                                          br_cdf_arr br_cdf
+#if DEBUG_BLOCK_INFO
+                                          , const AV1_COMMON *const cm,
+                                          const int mi_row, const int mi_col,
+                                          const int plane, const int size_ctx
+#endif
+                                          ) {
   for (int c = start_si; c <= end_si; c++) {
     const int pos = scan[c];
     const int coeff_ctx = get_upper_levels_ctx_2d(levels, pos, bwl);
     const int nsymbs = 4;
     int level = aom_read_symbol(r, base_cdf[coeff_ctx], nsymbs,
                                 ACCT_INFO("level", "base_cdf"));
+    int br_ctx = -1;
     if (level > NUM_BASE_LEVELS) {
-      const int br_ctx = get_br_ctx_skip(levels, pos, bwl);
+      br_ctx = get_br_ctx_skip(levels, pos, bwl);
       aom_cdf_prob *cdf = br_cdf[br_ctx];
       level += read_low_range(r, cdf);
     }
+      DEBUG_CF_printf("Post-tok[pos=%d,ctx=%d|%d|%d,plane=%s,%d]: r=%d\n",
+                      c, size_ctx, coeff_ctx, br_ctx,
+                      plane ? "uv" : "y", level, r->ec.rng);
     levels[get_padded_idx_left(pos, bwl)] = level;
   }
 }
@@ -330,6 +374,9 @@ static INLINE void read_coeffs_forward_2d(aom_reader *r, int start_si,
 // Decode the end-of-block syntax.
 static INLINE void decode_eob(DecoderCodingBlock *dcb, aom_reader *const r,
                               const int plane, const TX_SIZE tx_size
+#if DEBUG_BLOCK_INFO
+                              , const AV1_COMMON *const cm, int mi_row, int mi_col
+#endif
 #if CONFIG_PARAKIT_COLLECT_DATA
                               ,
                               const AV1_COMMON *const cm
@@ -461,6 +508,8 @@ static INLINE void decode_eob(DecoderCodingBlock *dcb, aom_reader *const r,
       break;
 #endif  // CONFIG_REDUCE_SYMBOL_SIZE
   }
+    DEBUG_CF_printf("Post-eob_bin_%d[ctx=%d,%d]: r=%d\n",
+                    16 << eob_multi_size, pl_ctx, eob_pt - 1, r->ec.rng);
 
   const int eob_offset_bits = av1_eob_offset_bits[eob_pt];
   if (eob_offset_bits > 0) {
@@ -482,6 +531,8 @@ static INLINE void decode_eob(DecoderCodingBlock *dcb, aom_reader *const r,
         aom_read_literal(r, eob_offset_bits - 1, ACCT_INFO("eob_extra"));
   }
   *eob = rec_eob_pos(eob_pt, eob_extra);
+  if (eob_offset_bits > 0)
+    DEBUG_CF_printf("Post-eob[%d]: r=%d\n", *eob - 1, r->ec.rng);
   *bob = *eob;  // escape character
   if (plane == AOM_PLANE_U) {
     xd->eob_u = *eob;
@@ -510,10 +561,11 @@ uint8_t av1_read_sig_txtype(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
   }
   int txb_skip_ctx = txb_ctx->txb_skip_ctx;
   int all_zero;
+  int pred_mode_ctx = -1;
   if (plane == AOM_PLANE_Y || plane == AOM_PLANE_U) {
     MB_MODE_INFO *const mbmi = xd->mi[0];
-    const int pred_mode_ctx =
-        (is_inter || mbmi->fsc_mode[xd->tree_type == CHROMA_PART]) ? 1 : 0;
+    pred_mode_ctx =
+      (is_inter || mbmi->fsc_mode[xd->tree_type == CHROMA_PART]) ? 1 : 0;
     all_zero = aom_read_symbol(
         r, ec_ctx->txb_skip_cdf[pred_mode_ctx][txs_ctx][txb_skip_ctx], 2,
         ACCT_INFO("all_zero", "plane_y_or_u"));
@@ -522,6 +574,11 @@ uint8_t av1_read_sig_txtype(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
     all_zero = aom_read_symbol(r, ec_ctx->v_txb_skip_cdf[txb_skip_ctx], 2,
                                ACCT_INFO("all_zero", "plane_v"));
   }
+#if DEBUG_BLOCK_INFO
+    const int mi_row = blk_row + xd->mi_row, mi_col = blk_col + xd->mi_col;
+    DEBUG_CF_printf("Post-all_zero[ctx=%d|%d|%d,%d]: r=%d\n",
+                    pred_mode_ctx, txs_ctx, txb_skip_ctx, all_zero, r->ec.rng);
+#endif
 
 #if CONFIG_INSPECTION
   MB_MODE_INFO *const mbmi = xd->mi[0];
@@ -560,6 +617,9 @@ uint8_t av1_read_sig_txtype(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
     return 0;
   }
   decode_eob(dcb, r, plane, tx_size
+#if DEBUG_BLOCK_INFO
+             , cm, mi_row, mi_col
+#endif
 #if CONFIG_PARAKIT_COLLECT_DATA
              ,
              cm
@@ -567,6 +627,9 @@ uint8_t av1_read_sig_txtype(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
   );
   av1_read_tx_type(cm, xd, blk_row, blk_col, tx_size, r, plane, *eob,
                    is_inter ? 0 : *eob);
+    DEBUG_CF_printf("Post-txtp[%d]: r=%d\n",
+                    xd->tx_type_map[blk_row * xd->tx_type_map_stride + blk_col],
+                    r->ec.rng);
 
   if (plane == AOM_PLANE_U && is_cctx_allowed(cm, xd)) {
     const int skip_cctx = is_inter ? 0 : (*eob == 1);
@@ -627,6 +690,9 @@ uint8_t av1_read_coeffs_txb_skip(const AV1_COMMON *const cm,
   eob_info *bob_data = dcb->bob_data[plane] + dcb->txb_offset[plane];
   bob_data->max_scan_line = 0;
 
+#if DEBUG_BLOCK_INFO
+    const int mi_row = xd->mi_row + blk_row, mi_col = xd->mi_col + blk_col;
+#endif
   const TX_TYPE tx_type =
       av1_get_tx_type(xd, plane_type, blk_row, blk_col, tx_size,
                       is_reduced_tx_set_used(cm, plane_type));
@@ -656,15 +722,23 @@ uint8_t av1_read_coeffs_txb_skip(const AV1_COMMON *const cm,
       int level = aom_read_symbol(r, cdf_bob, nsymbs_bob,
                                   ACCT_INFO("level", "cdf_bob")) +
                   1;
+      int br_ctx = -1;
       if (level > NUM_BASE_LEVELS) {
-        const int br_ctx = get_br_ctx_skip(levels, pos, bwl);
+        br_ctx = get_br_ctx_skip(levels, pos, bwl);
         aom_cdf_prob *cdf = br_cdf[br_ctx];
         level += read_low_range(r, cdf);
       }
+        DEBUG_CF_printf("Post-bob_tok[pos=%d,ctx=%d|%d|%d,plane=%s,%d]: r=%d\n",
+                        bob, size_ctx, coeff_ctx_bob, br_ctx,
+                        plane ? "uv" : "y", level, r->ec.rng);
       levels[get_padded_idx_left(pos, bwl)] = level;
     }
     read_coeffs_forward_2d(r, bob + 1, eob_data->eob - 1, scan, bwl, levels,
-                           base_cdf, br_cdf);
+                           base_cdf, br_cdf
+#if DEBUG_BLOCK_INFO
+                           , cm, mi_row, mi_col, plane, size_ctx
+#endif
+                           );
   }
 
   const int bob = av1_get_max_eob(tx_size) - bob_data->eob;
@@ -679,8 +753,14 @@ uint8_t av1_read_coeffs_txb_skip(const AV1_COMMON *const cm,
       int idtx_sign_ctx = get_sign_ctx_skip(signs, levels, pos, bwl);
       sign = aom_read_symbol(r, ec_ctx->idtx_sign_cdf[size_ctx][idtx_sign_ctx],
                              2, ACCT_INFO("sign"));
+        DEBUG_CF_printf("Post-sign[pos=%d,ctx=%d|%d,plane=%s,%d]: r=%d\n",
+                        c, size_ctx, idtx_sign_ctx, plane ? "uv" : "y", sign,
+                        r->ec.rng);
       signs[sign_idx] = sign > 0 ? -1 : 1;
       level = read_high_range(xd, r, 0, level, 0, &hr_level_avg
+#if DEBUG_BLOCK_INFO
+                              , cm, blk_row, blk_col, c
+#endif
 #if CONFIG_COEFF_BR_LF_UV_BYPASS
                               ,
                               plane
@@ -729,6 +809,8 @@ static INLINE tran_low_t read_coeff_hidden(aom_reader *r, TX_CLASS tx_class,
   const int pos = scan[0];
   int ctx_idx = get_base_ctx_ph(levels, pos, bwl, tx_class);
   q_index = aom_read_symbol(r, base_cdf_ph[ctx_idx], 4, ACCT_INFO("q_index"));
+    printf("Qindex[%d]: %d [r=%d]\n",
+           pos, q_index, r->ec.rng);
 
 #if !CONFIG_COEFF_BR_PH_BYPASS
   if (q_index > NUM_BASE_LEVELS) {
@@ -790,6 +872,9 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
           " blk_col = %d, plane = %d, tx_size = %d ",
           xd->mi_row, xd->mi_col, blk_row, blk_col, plane, tx_size);
 #endif
+#if DEBUG_BLOCK_INFO
+    const int mi_row = blk_row + xd->mi_row, mi_col = blk_col + xd->mi_col;
+#endif
 
   const TX_TYPE tx_type =
       av1_get_tx_type(xd, plane_type, blk_row, blk_col, tx_size,
@@ -842,6 +927,7 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
     const int col = pos - (row << bwl);
     int limits = get_lf_limits(row, col, tx_class, plane);
     if (plane > 0) {
+        int br_ctx = -1;
       if (limits) {
         aom_cdf_prob *cdf = ec_ctx->coeff_base_lf_eob_uv_cdf[coeff_ctx];
         level +=
@@ -850,7 +936,7 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
             1;
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx_lf_eob_chroma(pos, tx_class);
+          br_ctx = get_br_ctx_lf_eob_chroma(pos, tx_class);
           cdf = ec_ctx->coeff_br_lf_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
@@ -862,12 +948,16 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
                  1;
 
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = 0; /* get_lf_ctx_eob */
+          br_ctx = 0; /* get_lf_ctx_eob */
           cdf = ec_ctx->coeff_br_uv_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-eob_tok[pos=%d,ctx=%d|%d,freq=%s,plane=uv,%d]: r=%d\n",
+                        c, coeff_ctx, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     } else {
+        int br_ctx = -1;
       if (limits) {
         aom_cdf_prob *cdf = ec_ctx->coeff_base_lf_eob_cdf[txs_ctx][coeff_ctx];
         level += aom_read_symbol(r, cdf, LF_BASE_SYMBOLS - 1,
@@ -875,7 +965,7 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
                  1;
 
         if (level > LF_NUM_BASE_LEVELS) {
-          const int br_ctx = get_br_ctx_lf_eob(pos, tx_class);
+          br_ctx = get_br_ctx_lf_eob(pos, tx_class);
           cdf = ec_ctx->coeff_br_lf_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
@@ -886,11 +976,14 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
                  1;
 
         if (level > NUM_BASE_LEVELS) {
-          const int br_ctx = 0; /* get_lf_ctx_eob */
+          br_ctx = 0; /* get_lf_ctx_eob */
           cdf = ec_ctx->coeff_br_cdf[br_ctx];
           level += read_low_range(r, cdf);
         }
       }
+        DEBUG_CF_printf("Post-eob_tok[pos=%d,ctx=%d|%d|%d,freq=%s,plane=y,%d]: r=%d\n",
+                        c, txs_ctx, coeff_ctx, br_ctx, limits ? "lo" : "hi",
+                        level, r->ec.rng);
     }
     levels[get_padded_idx(pos, bwl)] = level;
     state = tcq_next_state(state, level);
@@ -920,7 +1013,11 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
                              br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-                             base_uv_cdf, br_uv_cdf, &state);
+                             base_uv_cdf, br_uv_cdf, &state
+#if DEBUG_BLOCK_INFO
+                             , cm, txs_ctx, mi_row, mi_col
+#endif
+                             );
       if (enable_parity_hiding) {
         for (int si = *eob - 1; si > 0; --si) {
           int pos = scan[si];
@@ -947,7 +1044,11 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
                             br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-                            base_uv_cdf, br_uv_cdf, &state);
+                            base_uv_cdf, br_uv_cdf, &state
+#if DEBUG_BLOCK_INFO
+                            , cm, txs_ctx, mi_row, mi_col
+#endif
+                            );
       }
     } else {
       read_coeffs_reverse(r, tx_class, 1, *eob - 2, scan, bwl, levels,
@@ -956,7 +1057,11 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
                           br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-                          base_uv_cdf, br_uv_cdf, &state);
+                          base_uv_cdf, br_uv_cdf, &state
+#if DEBUG_BLOCK_INFO
+                          , cm, txs_ctx, mi_row, mi_col
+#endif
+                          );
       if (enable_parity_hiding) {
         for (int si = *eob - 1; si > 0; --si) {
           int pos = scan[si];
@@ -983,7 +1088,11 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
 #if !CONFIG_COEFF_BR_LF_UV_BYPASS
                             br_lf_uv_cdf,
 #endif  // !CONFIG_COEFF_BR_LF_UV_BYPASS
-                            base_uv_cdf, br_uv_cdf, &state);
+                            base_uv_cdf, br_uv_cdf, &state
+#if DEBUG_BLOCK_INFO
+                            , cm, txs_ctx, mi_row, mi_col
+#endif
+                            );
       }
     }
   }
@@ -1013,9 +1122,13 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
                 r,
                 ec_ctx->dc_sign_cdf[plane_type][is_hidden ? 1 : 0][dc_sign_ctx],
                 2, ACCT_INFO("sign", "dc_sign_cdf", "plane_y_or_u"));
+              DEBUG_CF_printf("Post-dc_sign[pos=%d,ctx=%d,%d]: r=%d\n",
+                              c, dc_sign_ctx, sign, r->ec.rng);
           } else {
             sign = aom_read_literal(
                 r, 1, ACCT_INFO("sign", "dc_sign_cdf", "plane_y_or_u"));
+              DEBUG_CF_printf("Post-dc_sign[pos=%d,%d]: r=%d\n",
+                              c, sign, r->ec.rng);
           }
 #else
           sign = aom_read_symbol(
@@ -1027,6 +1140,8 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
 #if CONFIG_BY_PASS_V_SIGN
           sign = aom_read_literal(
               r, 1, ACCT_INFO("sign", "v_dc_sign_cdf", "plane_v"));
+            DEBUG_CF_printf("Post-dc_sign[pos=%d,%d]: r=%d\n",
+                            c, sign, r->ec.rng);
 #else
           int32_t tmp_sign = 0;
           if (c < xd->eob_u) tmp_sign = xd->tmp_sign[tmp_sign_idx];
@@ -1038,11 +1153,15 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
         if (plane == AOM_PLANE_U) xd->tmp_sign[tmp_sign_idx] = (sign ? 2 : 1);
       } else {
         sign = aom_read_bit(r, ACCT_INFO("sign"));
+          DEBUG_CF_printf("Post-sign[pos=%d,%d]: r=%d\n",
+                          c, sign, r->ec.rng);
       }
       if (is_hidden && c == 0) {
 #if CONFIG_COEFF_BR_PH_BYPASS
         if (level >= ((NUM_BASE_LEVELS + 1) << 1)) {
           int hr_level = (read_adaptive_hr(xd, r, hr_level_avg >> 1) << 1);
+            DEBUG_CF_printf("Post-residual[pos=%d,%d->%d]: r=%d\n",
+                            c, pos, hr_level, r->ec.rng);
           level += hr_level;
           hr_level_avg = (hr_level_avg + hr_level) >> 1;
         }
@@ -1056,6 +1175,9 @@ uint8_t av1_read_coeffs_txb(const AV1_COMMON *const cm, DecoderCodingBlock *dcb,
       } else {
         int limits = get_lf_limits(row, col, tx_class, plane);
         level = read_high_range(xd, r, tcq_mode, level, limits, &hr_level_avg
+#if DEBUG_BLOCK_INFO
+                                , cm, mi_row, mi_col, c
+#endif
 #if CONFIG_COEFF_BR_LF_UV_BYPASS
                                 ,
                                 plane
@@ -1160,6 +1282,12 @@ void av1_read_coeffs_txb_facade(const AV1_COMMON *const cm,
                   && cm->seq_params.enable_fsc_residual
 #endif  // CONFIG_FSC_RES_HLS
   );
+#if DEBUG_BLOCK_INFO
+    const int mi_row = xd->mi_row + row, mi_col = xd->mi_col + col;
+    DEBUG_CF_printf("decode_cf[y=%d,x=%d,pl=%d,tx=%dx%d]: r=%d\n",
+                    xd->mi_row + row, xd->mi_col + col, plane,
+                    tx_size_wide[tx_size], tx_size_high[tx_size], r->ec.rng);
+#endif
 
   const uint8_t decode_rest =
       av1_read_sig_txtype(cm, dcb, r, row, col, plane, &txb_ctx, tx_size);
-- 
2.48.1

